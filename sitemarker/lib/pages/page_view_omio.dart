import 'package:flutter/material.dart';

class PageViewOmio extends StatelessWidget {
  final String path;

  const PageViewOmio({super.key, required this.path});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
