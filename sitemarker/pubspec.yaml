name: sitemarker
description: "A beautiful, offline-first digital vault for your bookmarks."

publish_to: "none"

version: 4.0.0+4000 # TODO: Don't forget the version updation!!

scripts:
  test-db: "flutter test test/integration_tests/db_testing"
  test-all: "flutter test"
  check: "dart format lib test && flutter analyze"
  coverage: "flutter test --coverage && dart run remove_from_coverage -f coverage/lcov.info -r '.g.dart$' -r '.drift.dart$'"

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.33.0
  drift_flutter: ^0.3.0
  path_provider: ^2.1.5
  sqlite3: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  drift_dev: ^2.33.0
  build_runner: ^2.15.0
  rps: ^0.10.1
  remove_from_coverage: ^2.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
